# Client folder

The React + Vite + Tailwind frontend for **Kase Brothers** is currently implemented at the workspace root.

This folder is created to match the requested full-stack structure:

- `/server` for backend API
- `/client` for frontend app

If you want, the existing frontend can be moved into this folder in a follow-up step.